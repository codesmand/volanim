// Example response object
const response = {
    choices: ['Option A', 'Option B', 'Option C']
};

// Assigning the first choice to a constant variable
const choice = response.choices[0];
console.log(choice); // Output: 'Option A'
